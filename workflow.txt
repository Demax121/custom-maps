Użyć composable aby i pinia aby utworzyć "map" insatnce dostępne dla każdego komponentu.

import { useLinkStore } from '../stores/linkStore'
const linkStore = useLinkStore()

const statisMapPath = null;
const dynamicMapPath = ref();

function getMapSettings(){
    
}



// Setting up the map layer
const fullmap = L.tileLayer(mapPath, {
  minZoom: settings.minZoom,
  maxZoom: settings.maxZoom,
  continuousWorld: false,
  noWrap: true,
});

// Initializing the map
const Worldmap = L.map("map", {
  layers: [fullmap],
  zoomSnap: 0.25,
  zoomControl: false,
}).setView([0, 0], 2);




1. Użytkownik po kliknięciu w link odsyłany jest na podgląd mapy (jeżeli w galerii link dynamicznie generowany jeżeli nie link bezpośredni)
2. Nazwa mapy wykorzystywana jest do pobrania następujących rzeczy w tej kolejności (tiles, mapData, overlaysData, markersData)
3. Tworzona jest mapa z danymi z mapData
4. Tworzone są i dodawane overlaye do mapy
5. Do overlayi dodawane są markery aby mogy pojawić się na mapie